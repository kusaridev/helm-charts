---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.guac.graphql_server.name }}
{{- if .Values.guac.graphql_server.annotations }}
  annotations:
{{ toYaml .Values.guac.graphql_server.annotations | indent 4 }}
{{- end }}
  labels:
    app: {{ .Values.guac.graphql_server.name }}
spec:
  replicas:  {{ .Values.guac.graphql_server.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.guac.graphql_server.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.guac.graphql_server.name }}
    spec:
      containers:
      - name: {{ .Values.guac.graphql_server.name }}
        {{- if .Values.guac.graphql_server.image.digest }}
        image: "{{ .Values.guac.graphql_server.image.repository }}@{{ .Values.guac.graphql_server.image.digest }}"
        {{- else }}
        image: "{{ .Values.guac.graphql_server.image.repository }}:{{ .Values.guac.graphql_server.image.tag | default .Chart.AppVersion}}"
        {{- end }}
        imagePullPolicy: "{{ .Values.guac.graphql_server.image.pullPolicy }}"
        command:
{{ toYaml .Values.guac.graphql_server.image.command | indent 10 }}
        workingDir: {{ .Values.guac.workingDir }}
{{- if .Values.guac.graphql_server.image.ports }}
        ports:
{{ toYaml .Values.guac.graphql_server.image.ports | indent 10 }}
{{- end }}
        volumeMounts:
          - name: guac-config
            mountPath: {{ .Values.guac.workingDir }}
            readOnly: true
{{- if .Values.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.imagePullSecrets | indent 8 }}
{{- end }}
      volumes:
        - name: guac-config
          configMap:
            name: guac-cm
