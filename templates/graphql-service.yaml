# TO-DO: generate service based on values.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.guac.graphql_server.name }}
  labels:
    app: {{ .Values.guac.graphql_server.name }}
spec:
  selector:
    app: {{ .Values.guac.graphql_server.name }}
  ports:
    # TO-DO: generate this from the list of ports
    - protocol: TCP
      port: 8080
      targetPort: 8080
